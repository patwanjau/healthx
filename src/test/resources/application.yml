app:
  keystore:
    password: healthXKSP@ssw0rd
    alias: healthXStore

spring:
  datasource:
    url: jdbc:h2:mem:healthx
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

